<div class="container">
  <div class="card">
    <form class="card-body" action="/admin/edit/{{post._id}}" method="post">
      <label class="form-text" for="titulo">Título</label>
      <input id="titulo" class="form-control" type="text" name="titulo" value="{{post.titulo}}">
      <label class="form-text" for="descricao"></label>
      <textarea id="descricao" class="form-control" name="descricao">{{post.descricao}}</textarea>
      <button class="btn btn-success" type="submit" name="button">Enviar</button>
    </form>
  </div>
</div>
