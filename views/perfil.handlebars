<div class="container">
  <div class="card">
    <div class="card card-body">
      {{#if user}}
      <p>{{user.nome}}</p>
      <p>{{user.email}}</p>
      <p>{{user.eAdmin}}</p>
      <a href="/users/edit"> <button class="btn btn-warning" type="button" name="button">Editar perfil</button> </a>
      {{else}}
      <p>Você não está cadastrado</p>
      {{/if}}
    </div>
  </div>
</div>
